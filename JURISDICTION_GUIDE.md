# Руководство по работе с подсудностью в IskBot

## Обзор

Модуль `jurisdiction.py` добавляет поддержку определения подсудности споров с учетом:
- Подсудности по умолчанию (по месту ответчика, ст. 35 АПК РФ)
- Договорной подсудности (ст. 37 АПК РФ)
- Возможности ручного выбора суда

## Как это работает

### 1. Автоматическое определение

При загрузке претензии бот:

1. **Парсит документ** и извлекает данные сторон
2. **Ищет условия о подсудности** в тексте договора/претензии:
   - "Подсудность споров - Арбитражный суд..."
   - "Споры разрешаются в..."
   - "Споры подсудны..."
   - "По месту нахождения истца/ответчика"

3. **Определяет суд** одним из способов:
   - Если найдена договорная подсудность → использует указанный суд
   - Если не найдена → определяет по адресу ответчика (ст. 35 АПК РФ)

### 2. Диалог с пользователем

Бот показывает найденную информацию и предлагает:

```
🏛 Определение подсудности

📍 Тип: По месту нахождения ответчика (ст. 35 АПК РФ)
🏛 Суд: Арбитражный суд Волгоградской области
📮 Адрес: 400005, г. Волгоград, ул. 7-й Гвардейской, д. 2

Подсудность определена верно?

[✅ Верно] [📝 Указать другой суд] [❓ По месту ответчика]
```

### 3. Варианты выбора

#### Вариант 1: Подтверждение
Пользователь нажимает **✅ Верно** → бот использует найденный суд

#### Вариант 2: По умолчанию
Пользователь нажимает **❓ По месту ответчика** → бот использует адрес ответчика для определения суда

#### Вариант 3: Другой суд
Пользователь нажимает **📝 Указать другой суд** → бот просит ввести название:

```
Введите название суда (например: Арбитражный суд Московской области):
```

Пользователь вводит, например: `Арбитражный суд города Москвы`

Бот пытается найти в базе:
- **Найдено** → использует полные данные (название + адрес)
- **Не найдено** → сохраняет введенное название, но предупреждает проверить адрес

## Примеры использования

### Пример 1: Стандартная подсудность

**Претензия:** Содержит адрес ответчика "г. Волгоград, ул. Советская, 10"

**Результат:**
- Тип: По месту ответчика (ст. 35 АПК РФ)
- Суд: Арбитражный суд Волгоградской области
- Уверенность: 95%

### Пример 2: Договорная подсудность

**Договор содержит:**
```
7. Разрешение споров
7.1. Все споры по настоящему Договору подлежат рассмотрению
в Арбитражном суде города Москвы.
```

**Результат:**
- Тип: Договорная подсудность (ст. 37 АПК РФ)
- Суд: Арбитражный суд города Москвы
- Уверенность: 90%
- Найдено в документе: "Все споры по настоящему Договору подлежат..."

### Пример 3: Неясная подсудность

**Претензия:** Адрес ответчика указан неполно или некорректно

**Результат:**
- Тип: По месту ответчика
- Суд: Арбитражный суд по месту нахождения ответчика
- Адрес: Адрес суда не определен
- Уверенность: 30%

Бот предложит пользователю указать суд вручную.

## API модуля

### Класс `JurisdictionDetector`

```python
from jurisdiction import JurisdictionDetector

detector = JurisdictionDetector()

# Определить подсудность из текста
info = detector.detect_jurisdiction(
    text="текст договора/претензии",
    plaintiff_address="адрес истца",
    defendant_address="адрес ответчика"
)

print(info.type)           # JurisdictionType.DEFENDANT_ADDRESS
print(info.court_name)     # "Арбитражный суд..."
print(info.court_address)  # "123456, г. ..."
print(info.confidence)     # 0.95
```

### Типы подсудности

```python
from jurisdiction import JurisdictionType

JurisdictionType.DEFENDANT_ADDRESS  # По месту ответчика (ст. 35 АПК РФ)
JurisdictionType.CONTRACTUAL        # Договорная (ст. 37 АПК РФ)
JurisdictionType.PLAINTIFF_ADDRESS  # По месту истца (редко)
JurisdictionType.CUSTOM            # Указано пользователем
```

### Структура `JurisdictionInfo`

```python
@dataclass
class JurisdictionInfo:
    type: JurisdictionType              # Тип подсудности
    court_name: str                     # Название суда
    court_address: str                  # Адрес суда
    confidence: float                   # Уверенность (0.0 - 1.0)
    extracted_text: Optional[str]       # Текст из договора (если найден)
    region: Optional[str]               # Регион (если определен)
```

## Поддерживаемые паттерны

Модуль распознает следующие формулировки в договорах:

✅ "Подсудность споров - Арбитражный суд Московской области"
✅ "Споры разрешаются в Арбитражном суде города Санкт-Петербурга"
✅ "Споры подлежат рассмотрению в Арбитражном суде Краснодарского края"
✅ "Споры подсудны Арбитражному суду Свердловской области"
✅ "По месту нахождения ответчика"
✅ "По месту нахождения истца"

## База судов

Модуль использует полную базу арбитражных судов РФ из `config.py`:
- 85 регионов России
- Полные названия судов
- Точные адреса для направления документов

## Расширение функциональности

### Добавление нового паттерна

Отредактируйте `jurisdiction.py`:

```python
class JurisdictionDetector:
    JURISDICTION_PATTERNS = [
        # ... существующие паттерны ...

        # Добавьте новый паттерн
        r'Ваш_паттерн_здесь',
    ]
```

### Добавление суда в базу

Отредактируйте `courts_database.json`:

```json
{
  "Ваш регион": {
    "name": "Арбитражный суд ...",
    "address": "Индекс, адрес..."
  }
}
```

## Тестирование

Для проверки работы модуля:

```python
from jurisdiction import JurisdictionDetector

# Тест 1: Договорная подсудность
text = """
7. РАЗРЕШЕНИЕ СПОРОВ
7.1. Все споры по настоящему Договору подлежат рассмотрению
в Арбитражном суде Московской области.
"""

detector = JurisdictionDetector()
info = detector.detect_jurisdiction(text, defendant_address="г. Волгоград")

assert info.type == JurisdictionType.CONTRACTUAL
assert "Московской области" in info.court_name
print("✅ Тест 1 пройден")

# Тест 2: По месту ответчика
info2 = detector._get_default_jurisdiction("г. Казань, ул. Ленина, 1")
assert "Татарстан" in info2.court_name
print("✅ Тест 2 пройден")
```

## FAQ

**Q: Что если суд не найден в базе?**
A: Бот сохранит введенное пользователем название, но попросит проверить адрес вручную в готовом документе.

**Q: Можно ли указать суд общей юрисдикции?**
A: Нет, бот работает только с арбитражными судами. Для судов общей юрисдикции используйте вариант "Указать другой суд".

**Q: Как изменить приоритет определения подсудности?**
A: Отредактируйте метод `detect_jurisdiction()` в `jurisdiction.py`, изменив порядок проверок.

**Q: Поддерживается ли подсудность по месту исполнения договора?**
A: Пока нет, но можно добавить паттерн для такой формулировки в `JURISDICTION_PATTERNS`.

## Логирование

Модуль логирует все действия:

```
INFO: Найдена договорная подсудность: Арбитражный суд города Москвы
INFO: Договорная подсудность не найдена, применяется ст. 35 АПК РФ
WARNING: Суд для региона 'Неизвестная область' не найден в базе
DEBUG: Найдено совпадение: Московская обл. -> Московская область
```

Логи сохраняются в `bot.log`.
