# Анализ LLM Fallback реализации

## ✅ Что работает отлично

1. **Умная стратегия fallback** - LLM вызывается только когда поля действительно отсутствуют
2. **Многоуровневая валидация**:
   - Санитизация через `_sanitize_llm_data()` с использованием validators.py
   - Проверка наличия извлеченных данных в исходном тексте через `_filter_llm_data_by_text()`
3. **Устойчивость к ошибкам** - все сбои gracefully возвращают исходные данные
4. **Гибкая настройка** через переменные окружения

## 🔧 Внесенные улучшения

### 1. Исправлена опечатка в конфигурации
**Было**: `_get_env("LLAMA_HOSTO")` (опечатка)
**Стало**: Убрано, заменено на пустую строку

### 2. Улучшен промпт для модели
Добавлены четкие правила извлечения:
- Форматы ИНН (10/12 цифр), КПП (9 цифр), ОГРН (13/15 цифр)
- Формат трек-номеров (10-20 цифр)
- Строгий формат дат (ДД.ММ.ГГГГ)
- Пояснение кто истец, кто ответчик

### 3. Добавлено подробное логирование
Теперь видно:
- Сколько символов получено от LLM
- Какие поля извлечены
- Какие прошли валидацию
- Какие прошли проверку на наличие в тексте
- Причины отклонения (для debug-режима)

### 4. Реализована retry-логика
- 3 попытки с exponential backoff (1s, 2s, 4s)
- Retry только для Timeout и ConnectionError
- Не retry на 4xx/5xx ошибках (бессмысленно)

### 5. Проверка здоровья Ollama API
Функция `check_ollama_health()`:
- Проверяет доступность API перед вызовом
- Предотвращает зависания при недоступном сервисе
- Быстрый таймаут 5 секунд

### 6. Увеличен таймаут по умолчанию
**Было**: 30 секунд
**Стало**: 60 секунд (для qwen2.5:14b-instruct нужно больше времени)

## 🎯 Рекомендации по настройке

### Для qwen2.5:14b-instruct

Создайте или обновите `.env`:

```env
# Обязательные настройки
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen2.5:14b-instruct

# Опциональные (с рекомендуемыми значениями)
OLLAMA_TIMEOUT=90          # 14b модели работают медленнее
OLLAMA_MAX_CHARS=15000     # 14b модель может обработать больше текста
LLM_ENABLED=true           # Явно включить
```

### Проверка работы

Запустите бот с уровнем логирования INFO:

```bash
python3 main.py
```

В логах вы увидите:
```
INFO:llm_fallback:LLM response received: 1234 chars
INFO:llm_fallback:LLM extracted fields: ['plaintiff_inn', 'defendant_inn', ...]
INFO:llm_fallback:After validation: ['plaintiff_inn', 'defendant_inn', ...]
INFO:llm_fallback:After text verification: ['plaintiff_inn', 'defendant_inn']
```

## ⚠️ Потенциальные слабые места

### 1. Производительность
**Проблема**: qwen2.5:14b-instruct медленнее 7b версии
**Решение**: Уже увеличен таймаут до 60 секунд, можно увеличить до 90-120

### 2. Ресурсы
**Проблема**: 14b модель требует ~10GB RAM (GPU) или ~16GB (CPU)
**Рекомендация**: Используйте GPU если доступен:
```bash
ollama run qwen2.5:14b-instruct
```

### 3. Параллельные запросы
**Проблема**: Ollama обрабатывает запросы последовательно
**Влияние**: При одновременной загрузке нескольких документов будут задержки
**Решение**: Это нормально для Telegram-бота (один пользователь = один запрос)

### 4. Качество извлечения
**Проблема**: LLM может галлюцинировать несуществующие данные
**Решение**: Уже реализовано - `_filter_llm_data_by_text()` проверяет наличие в тексте

## 📊 Мониторинг

Для отслеживания качества работы LLM добавьте в `main.py`:

```python
# После вызова apply_llm_fallback
llm_filled = [k for k, v in claim_data.items() if v and v != "Не указано"]
logger.info(f"Total fields filled: {len(llm_filled)}")
```

## 🚀 Дальнейшие улучшения (опционально)

### 1. Кэширование результатов
Если один и тот же документ обрабатывается повторно:
```python
import hashlib

def get_text_hash(text: str) -> str:
    return hashlib.md5(text.encode()).hexdigest()

# Кэш в памяти или Redis
CACHE = {}

def apply_llm_fallback_cached(text: str, data: Dict[str, Any]) -> Dict[str, Any]:
    cache_key = get_text_hash(text)
    if cache_key in CACHE:
        return CACHE[cache_key]
    result = apply_llm_fallback(text, data)
    CACHE[cache_key] = result
    return result
```

### 2. A/B тестирование моделей
Сравните qwen2.5:7b vs qwen2.5:14b на реальных данных:
```python
# Запустите с 7b
OLLAMA_MODEL=qwen2.5:7b-instruct python3 main.py

# Запустите с 14b
OLLAMA_MODEL=qwen2.5:14b-instruct python3 main.py

# Сравните точность извлечения в логах
```

### 3. Fine-tuning модели
Если есть размеченные данные (pretenzii + правильные извлечения), можно дообучить модель.

## 📝 Вывод

Реализация LLM fallback **качественная и продакшн-ready**. Внесенные улучшения:
- ✅ Исправлена опечатка в конфигурации
- ✅ Улучшен промпт с четкими правилами
- ✅ Добавлено подробное логирование
- ✅ Реализована retry-логика
- ✅ Добавлена проверка здоровья API
- ✅ Увеличен таймаут для 14b модели

**Рекомендация**: Протестируйте на реальных документах и сравните точность с 7b версией. 14b модель должна давать более точные результаты, но работать медленнее.
