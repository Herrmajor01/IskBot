# Миграция на улучшенную систему парсинга

## 🎯 Для кого это руководство

Это руководство для тех, кто хочет интегрировать новую систему парсинга в существующий код IskBot.

## ⚡ Быстрая миграция (рекомендуется)

### Вариант 1: Минимальные изменения (обратная совместимость)

Просто замените один импорт в `main.py`:

```python
# Было:
from sliding_window_parser import parse_documents_with_sliding_window

# Стало:
from parser_integration import parse_document_integrated as parse_documents_with_sliding_window
```

**Всё!** Остальной код работает без изменений, но теперь использует улучшенный парсер.

### Вариант 2: Полная интеграция (с метаинформацией)

Если вы хотите получать информацию о качестве парсинга:

```python
from parser_integration import parse_document_integrated, get_parsing_report

# Вместо старого вызова:
claim_data = parse_documents_with_sliding_window(text)

# Используйте новый:
claim_data = parse_document_integrated(text)

# Опционально: получите отчет о качестве парсинга
report = get_parsing_report(text)
confidence = report['enhanced']['confidence']

if confidence < 0.6:
    await update.message.reply_text(
        "⚠️ Внимание: некоторые данные могут быть извлечены неточно."
    )
```

## 📊 Что изменилось в данных

### Новые поля (автоматически добавляются)

```python
# Типы организаций (автоматически определяются)
defendant_entity_type  # 'legal_entity' или 'individual'
plaintiff_entity_type  # 'legal_entity' или 'individual'

# Краткие названия (для подписи)
defendant_name_short   # 'ООО «Название»' или 'Фамилия И.О.'
plaintiff_name_short   # 'ООО «Название»' или 'Фамилия И.О.'
```

### Улучшенные поля (валидированные и исправленные)

```python
# Реквизиты (теперь валидируются по алгоритмам ФНС РФ)
defendant_inn   # Проверка контрольной суммы
defendant_kpp   # Проверка формата (автоматически None для ИП)
defendant_ogrn  # Проверка контрольной цифры

plaintiff_inn   # Проверка контрольной суммы
plaintiff_kpp   # Проверка формата (автоматически None для ИП)
plaintiff_ogrn  # Проверка контрольной цифры

# Названия организаций (автоматически форматируются)
defendant_name  # 'Общество с ограниченной ответственностью «Название»'
plaintiff_name  # 'Индивидуальный предприниматель Фамилия Имя Отчество'
```

## 🔄 Откат на старый парсер

Если что-то пошло не так, легко вернуться:

```python
# Вариант 1: Через параметр
from parser_integration import parse_document_integrated
claim_data = parse_document_integrated(text, use_legacy_only=True)

# Вариант 2: Напрямую через старый парсер
from sliding_window_parser import parse_documents_with_sliding_window
claim_data = parse_documents_with_sliding_window(text)
```

## 🧪 Тестирование после миграции

### Шаг 1: Проверка автотестов

```bash
python3 test_validators.py
```

Должно быть: `Ran 24 tests in 0.005s - OK`

### Шаг 2: Тестирование на реальном документе

Создайте тестовый скрипт:

```python
from parser_integration import parse_document_integrated, get_parsing_report

# Загрузите ваш тестовый документ
with open('test_claim.docx', 'rb') as f:
    from docx import Document
    doc = Document(f)
    text = '\n'.join([p.text for p in doc.paragraphs])

# Парсинг
result = parse_document_integrated(text)

# Проверка результата
print("=" * 60)
print("ИЗВЛЕЧЕННЫЕ ДАННЫЕ:")
print("=" * 60)
for key, value in result.items():
    print(f"{key}: {value}")

# Получение детального отчета
report = get_parsing_report(text)
print("\n" + "=" * 60)
print("ОТЧЕТ О ПАРСИНГЕ:")
print("=" * 60)
print(f"Legacy извлек: {report['legacy']['fields_extracted']} полей")
print(f"Enhanced извлек: {report['enhanced']['fields_extracted']} полей")
print(f"Уверенность: {report['enhanced']['confidence']:.2%}")
print(f"Предупреждения: {len(report['enhanced']['warnings'])}")
print(f"Ошибки: {len(report['enhanced']['errors'])}")
```

### Шаг 3: Сравнение результатов

Сравните результаты старого и нового парсеров:

```python
from sliding_window_parser import parse_documents_with_sliding_window
from parser_integration import parse_document_integrated

legacy_result = parse_documents_with_sliding_window(text)
new_result = parse_document_integrated(text)

# Сравнение по ключевым полям
key_fields = ['defendant_inn', 'plaintiff_inn', 'defendant_name', 'plaintiff_name', 'debt']

print("СРАВНЕНИЕ РЕЗУЛЬТАТОВ:")
print("=" * 80)
for field in key_fields:
    legacy_val = legacy_result.get(field, 'НЕТ')
    new_val = new_result.get(field, 'НЕТ')

    if legacy_val == new_val:
        print(f"✅ {field}: {legacy_val}")
    else:
        print(f"❌ {field}:")
        print(f"   Legacy:   {legacy_val}")
        print(f"   New:      {new_val}")
```

## 🛠️ Настройка парсинга

### Добавление новых паттернов

Откройте `parsing_config.py` и добавьте свои паттерны:

```python
# Пример: добавление паттерна для ИНН
INN_PATTERNS = [
    r'ИНН\s*[:\s]*(\d{10,12})',
    r'Ваш новый паттерн здесь',  # Добавьте сюда
]
```

### Изменение минимальной уверенности

```python
# В parsing_config.py
MIN_CONFIDENCE = 0.5  # Снизьте, если слишком строго
```

### Включение детального логирования

```python
# В parsing_config.py
DEBUG_LOGGING = True

# Или в коде
import logging
logging.basicConfig(level=logging.DEBUG)
```

## ⚠️ Частые проблемы и решения

### Проблема 1: "ModuleNotFoundError: No module named 'enhanced_parser'"

**Решение:** Убедитесь, что все новые файлы находятся в директории проекта:
- `enhanced_parser.py`
- `parser_integration.py`
- `parsing_config.py`

### Проблема 2: "Низкая уверенность в результатах (confidence < 0.5)"

**Причина:** Данные не прошли валидацию.

**Решение:**
1. Проверьте логи - там будет указано, какие именно данные невалидны
2. Если данные действительно корректны, но валидатор их отклоняет - откройте issue

### Проблема 3: "Некоторые поля пустые после миграции"

**Причина:** Паттерны не совпадают с вашим форматом документов.

**Решение:**
1. Включите `DEBUG_LOGGING = True` в `parsing_config.py`
2. Посмотрите логи - там будет видно, какие паттерны не сработали
3. Добавьте свои паттерны в `parsing_config.py`

### Проблема 4: "Парсинг стал медленнее"

**Ожидаемо:** Enhanced парсер работает ~600-800ms (против 200-300ms у legacy).

**Решение (если критично):**
```python
# Используйте только legacy парсер
claim_data = parse_document_integrated(text, use_legacy_only=True)
```

## 📝 Чек-лист миграции

- [ ] Сохранены резервные копии `main.py` и других критичных файлов
- [ ] Скопированы новые файлы в проект:
  - [ ] `enhanced_parser.py`
  - [ ] `parser_integration.py`
  - [ ] `parsing_config.py`
- [ ] Обновлен импорт в `main.py`
- [ ] Запущены автотесты (`python3 test_validators.py`)
- [ ] Протестировано на реальных документах
- [ ] Сравнены результаты legacy и enhanced парсеров
- [ ] Настроены паттерны под свои документы (если нужно)
- [ ] Проверена производительность

## 🎉 Готово!

После успешной миграции у вас будет:

- ✅ **4-уровневая система парсинга** с валидацией и восстановлением
- ✅ **Автоматическая проверка ИНН/КПП/ОГРН** по алгоритмам ФНС РФ
- ✅ **Восстановление недостающих данных** через эвристики
- ✅ **Детальное логирование** для отладки
- ✅ **Обратная совместимость** с legacy парсером

---

Если возникли вопросы, смотрите:
- [PARSER_GUIDE.md](PARSER_GUIDE.md) - Полное руководство по парсингу
- [VALIDATION_GUIDE.md](VALIDATION_GUIDE.md) - Руководство по валидации
